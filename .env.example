# Public origin — used for canonical URLs, sitemap.xml, robots.txt and OG tags.
# Update src/lib/site.ts if the production domain differs from corehold.com.
NEXT_PUBLIC_SITE_URL=https://corehold.com

# Audit request delivery (see src/app/api/audit-request/route.ts).
# Add whichever of these your chosen transport needs, then complete the TODO
# at the bottom of that route handler.
# RESEND_API_KEY=
# AUDIT_INBOX=audit@corehold.com
# HUBSPOT_ACCESS_TOKEN=
